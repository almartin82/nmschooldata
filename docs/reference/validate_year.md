# Validate year parameter

Validate year parameter

## Usage

``` r
validate_year(end_year, allow_future = FALSE)
```

## Arguments

- end_year:

  School year end to validate

- allow_future:

  If TRUE, allows years up to current + 1

## Value

TRUE if valid, otherwise throws error
