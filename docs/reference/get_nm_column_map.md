# Get column mapping for NM PED enrollment data

Returns mappings from NM PED column names to standardized names. NM PED
uses various column naming conventions across years.

## Usage

``` r
get_nm_column_map()
```

## Value

Named list of column mappings
